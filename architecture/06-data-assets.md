# Data Assets

153 типа (`DA_*`) — не строки, а один синглтон-объект на класс с глобальными настройками системы.

`list_data_asset_types` · `get_data_asset_schema` · `get_data_asset_values` · `set_data_asset_fields`

Открываются как воркспейс с `book_class='DA'`. Полная карта типов — в
{doc}`../case-studies/data-hierarchy`.
